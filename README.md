# 🇹🇭 Thai Travel AI Dashboard

**ระบบวิเคราะห์สถิติและพยากรณ์การท่องเที่ยวไทยด้วย AI**

โปรเจกต์นี้เป็น Web Application ที่พัฒนาด้วย Python และ Streamlit เพื่อวิเคราะห์ข้อมูลการท่องเที่ยวของประเทศไทย โดยใช้ Machine Learning ในการพยากรณ์จำนวนนักท่องเที่ยวในอนาคต และวิเคราะห์ปัจจัยเชิงลึก (SWOT Analysis) เพื่อเสนอแนะแนวทางพัฒนาสำหรับแต่ละจังหวัด

## 🚀 ฟีเจอร์หลัก (Key Features)

### 1. 📊 พยากรณ์รายจังหวัด (Forecast)
* แสดงข้อมูลสถิตินักท่องเที่ยวย้อนหลัง (Historical Data)
* **Time Series Forecasting:** ใช้โมเดล **Prophet** พยากรณ์จำนวนนักท่องเที่ยวล่วงหน้า 12 เดือน
* แสดงผลกราฟแนวโน้ม (Trend) และรูปแบบตามฤดูกาล (Seasonality)
* เลือกดูได้ทั้งภาพรวมระดับประเทศ และเจาะจงรายจังหวัด

### 2. 🧠 วิเคราะห์ปัจจัยและ SWOT (AI Model)
* **Machine Learning:** ใช้โมเดล **Random Forest Regressor** วิเคราะห์ความสัมพันธ์ระหว่างปัจจัยต่างๆ (เช่น การค้นหาใน Social Media, จำนวนสถานที่ท่องเที่ยว, ปัญหาเศรษฐกิจ) กับจำนวนนักท่องเที่ยว
* **Global Insights:** แสดงปัจจัยที่มีอิทธิพลต่อการท่องเที่ยวมากที่สุด
* **Auto SWOT Analysis:** สร้างบทวิเคราะห์จุดแข็ง (Strengths), จุดอ่อน (Weaknesses), และโอกาส (Opportunities) ของแต่ละจังหวัดให้อัตโนมัติ
* **Action Plan:** แนะนำกลยุทธ์ที่ปรับปรุงได้จริง (Actionable Factors) โดยเรียงลำดับตามความสำคัญ

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

* **Python 3.x**
* **Streamlit:** สำหรับสร้าง User Interface (Dashboard)
* **Pandas & NumPy:** สำหรับจัดการและประมวลผลข้อมูล
* **Prophet:** สำหรับพยากรณ์ข้อมูลอนุกรมเวลา (Time Series)
* **Scikit-learn:** สำหรับสร้างโมเดล Random Forest
* **Plotly:** สำหรับสร้างกราฟ Interactive

## ⚙️ การติดตั้งและใช้งาน (Installation)

1.  **Clone หรือดาวน์โหลดโปรเจกต์นี้**
    ```bash
    git clone [https://github.com/your-username/thai-travel-dashboard.git](https://github.com/your-username/thai-travel-dashboard.git)
    cd thai-travel-dashboard
    ```

2.  **ติดตั้ง Library ที่จำเป็น**
    แนะนำให้สร้าง Virtual Environment ก่อน แล้วรันคำสั่ง:
    ```bash
    pip install -r requirements.txt
    ```

3.  **รันโปรแกรม**
    ```bash
    streamlit run dashboard.py
    ```

4.  **เปิดใช้งาน**
    ระบบจะเปิด Browser ขึ้นมาอัตโนมัติที่ `http://localhost:8501`

## 📂 โครงสร้างไฟล์

* `dashboard.py`: ไฟล์หลักสำหรับรันแอปพลิเคชัน (Source Code)
* `requirements.txt`: รายชื่อ Library ที่ต้องติดตั้ง
* `README.md`: เอกสารอธิบายโปรเจกต์

## 📝 หมายเหตุเกี่ยวกับข้อมูล

* ระบบดึงข้อมูลอัตโนมัติจาก Google Drive (CSV Online) ดังนั้น **จำเป็นต้องเชื่อมต่ออินเทอร์เน็ต** ขณะใช้งาน
* ข้อมูลสถิตินักท่องเที่ยวและข้อมูลปัจจัยต่างๆ ได้รับการทำความสะอาด (Data Cleaning) และกรองข้อมูลที่ไม่ใช่จังหวัดออกภายในโค้ดแล้ว